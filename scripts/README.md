# 辅助脚本

项目级脚本（环境检查、一键冒烟等），与上游 `microduck_rl` 仓库脚本区分。

| 脚本 | 说明 | 状态 |
|------|------|------|
| `check-gpu.ps1` | Windows 检查 1060 + PyTorch | 📝 |
| `smoke-train.ps1` | 调用 microduck_rl 冒烟 | 📝 |

上游训练命令以 `vendor/microduck_rl` 为准。
